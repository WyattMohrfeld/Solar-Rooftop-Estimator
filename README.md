# Solar Rooftop Estimator (MVP)

Enter an address and an estimated roof area to get a rough solar system size and installed cost range.

## Run locally
```bash
pip3 install -r requirements.txt
streamlit run app.py
